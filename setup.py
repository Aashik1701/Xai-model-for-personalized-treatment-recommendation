#!/usr/bin/env python3
"""
Minimal setup.py for package building only.

For project setup and environment configuration, use:
    python project_setup.py
"""

from setuptools import setup

# This minimal setup.py is used only for package building.
# All configuration is in pyproject.toml
# For interactive project setup, use project_setup.py instead

if __name__ == "__main__":
    setup()
